{
  "name": "lastfm-to-ytm",
  "version": "0.1.0",
  "description": "Last.fm → YouTube Music Playlist Creator - JS tooling",
  "private": true,
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --write --unsafe",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.6"
  }
}
